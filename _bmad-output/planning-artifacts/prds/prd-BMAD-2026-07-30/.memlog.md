---
topic: BMAD Idea Launcher v2 PRD (favoriting + real LLM backend)
updated: 2026-07-30T16:15
---

- (decision) Scope confirmed by user: v2 PRD covers FR-4 favoriting (completing v1's original spec) and closing the FR-2 gap with a real LLM-backed Idea Generation Service. PromptGateway wiring explicitly excluded as a separate future decision, not committed here. Fast path drafting.
- (decision) LLM backend: provider-agnostic in the PRD, key via server-side env var only. Favoriting stays session-only (no localStorage) per user choice - matches v1's original FR-4 spec exactly.
- (event) Draft prd.md written: FR-1 (real LLM backend, provider-agnostic) + FR-2 (favoriting, session-only). Flagged pre-existing architecture/epics artifacts referencing this same scope as an open reconciliation question, per user's explicit choice to continue despite the likely duplication.
- (change) Input reconciliation against brief-BMAD-2026-07-30 found 3 gaps: dropped working-tree-vs-committed caveat, dropped absence-is-not-proof caveat, and narrowed brief's Open Question 1 (story-doc staleness) down to just the provider/model sub-detail. All 3 fixed: caveats restored in FR-1/FR-2 descriptions, staleness question restored as its own Open Question.
- (change) Rubric review (review-rubric.md): strong overall, 1 high + 1 medium finding, both fixed. Reordered Open Questions so the pre-existing-artifacts reconciliation risk leads (was #4, now #1); gave SM-1 a concrete 30s bound instead of vague 'reasonable time'.
- (decision) Phase-blocker triage: user chose to defer OQ1 (pre-existing architecture/epics reconciliation) rather than resolve now. Owner: whoever runs bmad-architecture or bmad-create-epics-and-stories next. Revisit condition: before generating architecture/epics from this PRD, first restore & check _bmad-output/planning-artifacts/architecture/architecture-BMAD-2026-07-29/ and any epics.md from git history to confirm this PRD doesn't duplicate/conflict with already-approved work.
- (event) Polish pass complete (structure + prose); confidence scored 55/Medium, capped by the deliberately-deferred pre-existing-artifacts reconciliation question.
- (event) PRD approved (v1) by Mel.
- (decision) Open Question 1 investigated: restored aa9f329's ARCHITECTURE-SPINE.md from git history and confirmed it already fully covers this PRD's scope (AD-2 LLM adapter, AD-4 favoriting, AD-5/AD-6 fixed contract), built from prd-BMAD-2026-07-27 binding FR-1-4. User was informed of the redundancy and explicitly chose to generate a new architecture pass anyway rather than reuse the existing one.
