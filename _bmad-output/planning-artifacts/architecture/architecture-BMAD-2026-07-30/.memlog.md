---
scope: BMAD Idea Launcher v2: real LLM-backed idea generation + favoriting (FR-1, FR-2 of prd-BMAD-2026-07-30)
purpose: build-substrate
altitude: feature
updated: 2026-07-30T16:31
---

- (decision) Fast path chosen. A near-identical prior architecture spine (aa9f329, approved v1, built from prd-BMAD-2026-07-27) already exists for this exact scope, restored from git history and read in full. User was informed of the redundancy and explicitly chose to generate a new spine anyway. This spine reuses that prior spine's paradigm and AD content as [ADOPTED] existing reality rather than re-deriving from scratch, retargeted to the new PRD's FR-1/FR-2 IDs.
- (decision) Paradigm [ADOPTED]: layered monolith, one process (presentation public/ -> server.js HTTP handler -> idea service adapter -> LLM provider). Ratified from existing server.js/public/ code, matching the prior spine's paradigm exactly.
- (decision) AD-1 [ADOPTED]: no web framework, Node core http only, binds all HTTP handling. Matches server.js as it exists.
- (decision) AD-2 [ADOPTED]: Idea Generation Service is a server-side-only adapter, binds FR-1 (real LLM backend). Key read only from process.env inside the adapter, never client-exposed. Env var name left unspecified per PRD's provider-agnostic decision (was IDEA_LLM_API_KEY in prior spine; kept as an [ASSUMPTION] here since new PRD deliberately didn't fix a name).
- (decision) AD-3 [ADOPTED]: all application state is client-side and session-only, binds FR-1, FR-2. Prompt/ideas/favorite live in one client state object in app.js; server stateless; controlled prompt input; new-generate clears ideas and resets favorite before fetch dispatches; Generate button disabled during in-flight request.
- (decision) AD-4 [ADOPTED]: favorite is single-select, client-owned, binds FR-2. One favoritedIndex: number|null held client-side; server has no favorite concept; clicking the favorited card deselects, clicking another moves the favorite.
- (decision) AD-5 [ADOPTED]: /api/ideas is the sole backend endpoint, fixed contract, binds FR-1 (30s target from PRD SM-1, inline error+retry). POST /api/ideas, request {prompt: string}, success 200 {ideas:[{title,description}]} exactly 3 entries. Errors {error:string}: 400 (invalid JSON/empty/>2000 chars), 504 (timeout, server-enforced AbortController at 25s), 502 (provider/network failure), 500 (malformed output, never padded/truncated/fabricated). Client issues no separate timeout; must not call for empty/whitespace prompt.
- (decision) AD-6 [ADOPTED]: handler/adapter interface fixed, binds FR-1, AD-2. module.exports.generateIdeas in lib/ideaService.js, signature (prompt: string) => Promise<Array<{title, description}>>. Failures signal only by throwing (never a resolved {error} envelope); .code in {TIMEOUT, NETWORK, MALFORMED} maps to AD-5 status codes. No caching/memoization ever (PRD SM-C1).
- (constraint) Consistency conventions [ADOPTED]: camelCase JS naming, idea shape always {title, description}; no ids/dates on wire; errors always {error:string}; favorite highlight conveyed by more than color alone (border/icon, per PRD FR-2); state mutation only in client's single state object; no auth; config via env vars only.
- (version) Stack [ADOPTED, unchanged from prior spine]: Node.js >=22, CommonJS, node --test, no web framework.
- (direction) Structural seed: server.js (handler), lib/ideaService.js (adapter, new), public/{index.html,app.js,styles.css}, test/. No new top-level directories.
- (question) Deferred: LLM provider/model choice (PRD Open Question 3), env var exact name (PRD leaves provider-agnostic, an open gap vs the prior spine's IDEA_LLM_API_KEY assumption), localStorage persistence, rate limiting/cost ceiling, PromptGateway integration (explicitly out of scope per PRD), deployment/ops (local-only learning exercise).
- (event) Spine distilled: ARCHITECTURE-SPINE.md written with AD-1 through AD-6, binding FR-1/FR-2 of prd-BMAD-2026-07-30. Leads with a highest-priority note flagging redundancy with the pre-existing approved spine.
- (change) Input reconciliation against prd-BMAD-2026-07-30 found 3 gaps, all fixed: AD-4's click-to-deselect behavior now flagged [ASSUMPTION] (invented, not stated in FR-2); AD-5's 25s/30s timeout now flagged [ASSUMPTION] carrying forward PRD Open Question 4's unresolved doubt; Deferred's provider/model note now names the concrete story-doc detail (Claude, IDEA_LLM_API_KEY, unconfirmed model ID) instead of a generic TBD.
- (change) Reviewer gate: lint clean after removing a placeholder 'TBD'; rubric PASS WITH FINDINGS (1 major/1 moderate/1 minor, all fixed); tech-currency found Node >=22 stale (bumped to >=24 per web research); adversarial found 2 critical + 2 high divergence holes, all fixed: handler forbidden from re-validating/truncating adapter output, handler forbidden from its own timeout (single AbortController owner), well-formed defined explicitly, env var read at module-load not per-call, explicit uncoded/unrecognized-code->500 fallback, 10KB body-size cap added, test-mocking convention added, AD-3 relabeled as a new decision not ratified reality, provider/env-var deferral given a non-binding default recommendation.
- (event) Spine finalized (status: final). Confidence scored 55/Medium. No additional rendering requested (spine only).
- (event) Canonical review-report.md written (architecture_version:1), synthesizing lint + rubric + tech-currency + adversarial passes, confirming all findings resolved except one low-severity error-string-content standard, left as-is.
- (event) Architecture spine approved (v1) by Mel.
